# Vendored single-source copies, generated by `npm run sync-shared` from shared/.
# linguist-generated: keep them out of language stats and the corpus checks that
#   reason about the project's own authored code (barriers' access scan included).
# merge=ours: a conflicting merge auto-resolves to our copy instead of a hand-edit
#   that would desync it from shared/ (see generated-merge-driver).
*.GENERATED.mjs linguist-generated merge=ours

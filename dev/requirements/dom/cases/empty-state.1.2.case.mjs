// 1.2 — A commentable page with no notes: page id in the header (mirrored in its title tooltip),
// the empty prompt, and the composer shown.
"use strict";

export default {
  description: "a commentable page with no notes shows the page id, the empty prompt, and the composer",
  tabUrl: "https://example.com/article",
  comments: [],
};

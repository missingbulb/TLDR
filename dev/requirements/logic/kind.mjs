// Kind: logic — a non-visual product/format rule or a static UI-surface declaration (the note
// time-formatting, the accessibility contract of the shipped HTML, the manifest's user-facing entry
// points). A wired case carries an executable verify() asserting the rule against the real shipped
// code/markup; a tbd case names where it's covered today (coveredBy). The owner-approved expected is
// the coded assertion. Runner: logic/logic.test.mjs.
"use strict";

export default { snapshot: false };
